{
  "name": "@policy-quote/infra",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --max-warnings=0",
    "synth": "cdk synth",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.0.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "aws-cdk": "^2.0.0",
    "esbuild": "^0.27.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
